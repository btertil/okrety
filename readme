Gra konsolowa polegająca na symulowanej walce między okrętami.
Nic wielkiego, krótki program w C++ z przykładem dziedziczenia klas.
Program uruchamia się w terminalu i nie ma żadnej grafiki.

Kompilacja pod windowsem: g++ okrety.cpp -o okrety.exe

Kompilacja pod linuxem: g++ okrety.cpp -o okrety
w przypadku linuksa można skorzystać z make
